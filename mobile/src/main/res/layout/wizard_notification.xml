<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingTop="48dp"
    android:paddingBottom="48dp"
    android:paddingRight="24dp"
    android:paddingLeft="24dp">

    <ImageView
        android:id="@+id/wizard_notification_explanation_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_arrow"/>

    <TextView
        android:id="@+id/wizard_notification_explanation_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/wizard_notification_explanation_text"
        android:textSize="28sp"
        android:textColor="@color/white"
        android:gravity="center"/>

    <ImageView
        android:id="@+id/wizard_notification_image"
        android:layout_width="156dp"
        android:layout_height="156dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:src="@drawable/wiz_notification"/>

</RelativeLayout>
